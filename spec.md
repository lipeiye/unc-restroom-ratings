# UNC Toilet 技术规范

状态：方案起草，待产品、校园数据和技术约束验证后冻结。

## 1. 产品定义

UNC Toilet 是校园厕所实时评测应用。它不是厕所社交网络，也不是复杂的卫生管理系统；它首先是一个“快速选择下一间厕所”的决策工具。

核心输出只有三类：

- 空间事实：厕所在哪里、是否可能开放、是否支持无障碍等。
- 即时判断：近期用户反馈所表达的当前使用体验。
- 行动入口：步行距离、预计时间和导航。

监测范围为校园内所有已登记厕所，默认每日 07:30 至次日 00:00。监测时段是数据和产品运营约束，不等同于保证每间厕所始终开放；开放状态必须允许按地点配置并显示数据来源/更新时间。

## 2. 第一性原理推导

### 2.1 从用户任务出发

用户在厕所选择场景中通常没有时间阅读长内容。其最小决策链是：

`我在哪里 -> 哪些厕所可去 -> 哪间现在更可靠 -> 怎么到达`

因此首屏必须优先呈现附近厕所、当前可用信息、距离/方向和导航动作。排行榜、历史趋势、个人资料等信息不能阻塞这条链。

### 2.2 从信息价值出发

一条信息只有在能改变选择时才值得占用屏幕和数据成本。把功能分成三层：

- 必须有：厕所目录、位置/距离、最新评分、更新时间、异常状态、导航。
- 有证据后再有：实时榜单、累计榜单、趋势、低样本提示、异常确认。
- 暂不做：复杂社交关系、长文本社区、积分商城、与如厕决策无关的个性化内容。

### 2.3 从信任出发

“实时”不是无限准确，而是让用户知道信息有多新、多可靠。每个动态结果至少需要：评分或状态、有效时间、样本量/可信度提示、必要时的数据缺失说明。过期信息不能伪装成当前事实。

### 2.4 从成本出发

评价行为的成本越低，反馈密度越高；反馈越多，实时判断越有用。因此使用固定且少量的评价选项，避免强制文字输入。低成本不意味着无约束：需要时间窗口、频率限制、异常检测和撤销/更正机制。

### 2.5 从风险出发

错误的“干净”判断主要造成选择不佳；错误的紧急异常警报可能造成恐慌、骚扰或资源浪费。异常状态需要更严格的证据、过期和审核策略，展示时必须带时间和状态，而不是永久标签。

## 3. 核心用户流程

### 3.1 找厕所与导航

1. 首次进入请求定位权限，并解释其用途。
2. 授权成功后按距离展示附近厕所，默认优先可用且数据新鲜者。
3. 每个厕所展示名称、步行距离/时间、近期评分、更新时间、异常警报和开放信息。
4. 用户点击明确的导航动作，交给已配置的地图服务；若不可用，至少展示地址和方向信息。
5. 定位失败时允许手动选择校园区域，不让整个应用失效。

### 3.2 评价

推荐先验证三级评价：`好`、`一般`、`差`。评价页还可以提供少量结构化状态：`有屎没冲`、`无纸`、`无水`、`堵塞`、`无法进入`。异常和普通环境评价分开确认，避免用户误把紧急状态当成低分。

提交后立即显示成功、记录更新时间和必要的冷却提示。用户重复评价时，不应制造多份独立样本；应采用短时间冷却、更新原评价或服务端去重中的一种明确策略。

### 3.3 异常生命周期

`上报 -> 待确认 -> 当前有效 -> 已解决/已过期 -> 归档`

“有屎没冲”默认属于高影响异常。服务端应保存上报时间、厕所、异常类型、最小反滥用信息和处理状态。前端显示“几分钟前上报”及“可能已解决”等不确定性。超过有效窗口自动过期；多次独立上报可提高可信度，但不能无限延长生命周期。

## 4. 信息架构

### 首页：附近

服务核心决策链。默认按可用性、距离和信息新鲜度综合排序；提供校园区域切换、刷新和导航入口。

### 厕所详情

显示位置、开放信息、无障碍信息、当前/近期评价、异常状态、更新时间、样本量和导航。历史内容用于解释趋势，不应压过当前状态。

### 当日实时评分榜

回答“现在去哪间更好”。只使用当日有效窗口内的反馈，并显示样本量、更新时间和低样本提示。榜单独立成模块，避免与累计表现混排。

### 累计评分榜

回答“长期哪间更稳定”。使用较长周期的聚合结果，必须显示统计周期、评分数量和数据覆盖程度，防止少量极端评价登顶。

### 我的/设置

管理定位权限说明、通知偏好、已提交反馈的状态、隐私说明和问题反馈。非核心入口不得占据首页主要空间。

## 5. 领域模型

### Toilet

`id`、`name`、`building`、`coordinates`、`campusArea`、`openingHours`、`accessibility`、`status`、`lastDirectoryUpdate`。

### Rating

`id`、`toiletId`、`score`、`createdAt`、`effectiveUntil`、`clientContext`（仅保留反滥用所需最小信息）、`status`。不公开用户身份。

### Alert

`id`、`toiletId`、`type`、`severity`、`reportedAt`、`expiresAt`、`confidence`、`status`、`resolutionSource`。

### Aggregate

`toiletId`、`window`、`score`、`sampleCount`、`coverage`、`calculatedAt`、`ruleVersion`。所有聚合结果必须带规则版本，便于回溯规则变化。

## 6. 评分与排序原则

### 6.1 评分

初期采用有限离散评分，不要求用户解释。聚合时至少考虑：

- 当日实时窗口与累计窗口分开计算。
- 时间衰减：近期反馈权重更高，但不能让单条反馈完全决定结果。
- 样本量：低样本结果必须标记，必要时使用保守估计。
- 异常：高影响异常影响即时可用性排序，但不应直接等价为长期环境分数。
- 冲突：同一时段相反反馈应降低确定性并展示“信息不一致”。

具体公式在获得真实数据后用离线样本验证；在公式冻结前，不允许客户端自行实现聚合逻辑。

### 6.2 附近排序

排序目标是“可行动的最佳选择”，不是单一最高分。建议输入：厕所是否开放、严重异常、距离、实时评分、新鲜度和样本可信度。必须定义不可用条件和并列规则，保证结果稳定可解释。

## 7. 系统架构边界

建议采用清晰分层：

- 客户端：展示状态、采集用户操作、请求定位、发起导航，不拥有权威评分规则。
- API/领域服务：校验请求、执行权限与频率限制、维护异常生命周期和返回统一状态。
- 数据层：厕所目录、原始评价、异常事件、聚合结果、审核/审计记录。
- 聚合任务：按规则版本计算实时和累计榜单；支持重算和回放。
- 管理端：维护厕所目录、审核异常、调整开放信息、查看审计记录。
- 观测层：错误、延迟、数据新鲜度、评分提交成功率和异常处理时效。

首版可以使用模块化单体加定时聚合任务，以降低部署和运维成本；只有在数据量或可靠性证明需要时再拆分服务。

## 8. 接口与一致性要求

- 所有动态接口返回 `updatedAt`、`expiresAt` 或等价的新鲜度信息。
- 写入接口具备幂等键，避免网络重试造成重复评分或重复警报。
- 读取接口明确区分 `available`、`stale`、`unknown`、`closed`、`alerted` 等状态。
- 服务端是评分、异常和榜单的唯一权威来源。
- 规则、状态枚举和错误码应版本化；客户端遇到未知枚举应安全降级。
- 位置数据按最小化原则处理；默认不保存精确轨迹。

## 9. 隐私、安全与滥用防护

- 定位只用于附近排序和导航准备，默认不建立持续轨迹。
- 评价匿名展示，服务端仅保留必要的速率限制、设备/会话一致性或审计信息。
- 对评分和异常分别限频，识别短时重复、批量刷分、异常地点切换和恶意举报。
- 管理操作需要权限控制、审计和可回滚记录。
- 不收集与功能无关的姓名、通讯录、照片或精确行为历史。
- 错误信息不泄露内部凭据、用户标识或管理规则细节。

## 10. 可用性与降级

必须覆盖：首次授权、拒绝定位、定位精度不足、无网络、接口超时、厕所无动态数据、厕所关闭、异常已过期、评分冲突、第三方地图不可用和服务端限频。

任何降级状态都要告诉用户当前能确定什么、不能确定什么，以及下一步可做什么。空状态不能只显示“暂无数据”；应提供手动选区、刷新或查看目录信息等动作。

## 11. 测试与验收

### 功能测试

验证目录、定位、附近排序、评分提交、幂等、异常生命周期、榜单分离、导航和管理审核。

### 数据测试

验证乱序事件、重复事件、低样本、过期、跨午夜窗口、规则版本重算和时区处理。监测时段按校园所在时区解释，不能依赖客户端本地时区。

### 体验测试

观察首次用户是否无需教程完成“找到厕所 -> 判断 -> 导航”和“完成评价/上报”。测量操作次数、完成耗时、误触率和状态理解度。

### 非功能测试

验证首屏延迟、并发写入、限频、权限隔离、日志脱敏、可访问性和网络不稳定下的恢复。

## 12. 版本推进原则

每次迭代按以下顺序：

1. 先验证用户是否真的需要该判断。
2. 先补齐事实来源和失效机制，再增加展示。
3. 先选择最小可解释规则，再用数据证明是否需要复杂化。
4. 先保护实时闭环和可信度，再扩展排行榜、社交或个性化。
5. 每次规则变化都增加 `ruleVersion`、迁移/重算方案和回滚方式。

## 13. 待验证问题

- UNC 校园地图、厕所目录和开放时间的权威来源。
- 校园建筑内定位精度与用户隐私边界。
- 学生对三级评分和结构化异常词汇的理解。
- 当日实时窗口和异常有效期的最佳参数。
- 管理员响应能力、审核 SLA 和通知渠道。
- 首版目标平台、地图服务条款、预算和部署环境。

在这些问题获得证据前，Agent 可以做原型和离线实验，但不得把实验结果当作线上承诺。
