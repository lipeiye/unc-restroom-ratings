# UNC Toilet Agent 协作规范

本文件定义不同 Agent 如何读取项目、如何分工、如何交接，以及如何使用 Skills 与 Memories。目标是让新 Agent 在最短时间内恢复准确上下文，不重复调查，也不把推测当成决策。

## 单一真相源与读取顺序

每次接手先按以下顺序读取：

1. `README.md`：确认当前阶段、已完成事项、阻塞项和下一步。
2. `spec.md`：确认产品目标、约束、数据规则和技术边界。
3. `agent.md`：确认自己的职责、交付格式和协作流程。
4. 与当前任务直接相关的代码、测试、配置、设计或研究材料。
5. 项目内最近的变更记录、决策记录和未关闭问题。

若文件之间冲突，优先级为：明确的用户最新要求 > 已验证的实现与测试结果 > `spec.md` 中的已批准规则 > `README.md` 状态描述 > Agent 推测。冲突不得静默覆盖，应在交付中报告并提出最小解决方案。

## Agent 角色边界

### 产品/研究 Agent

负责用户问题、校园场景、竞品/使用者调研、指标和需求拆解。不得直接改变技术契约或擅自扩大范围。交付必须区分事实、假设、问题和建议。

### 架构/技术规范 Agent

负责系统边界、领域模型、数据新鲜度、评分算法、异常生命周期、权限和非功能要求。任何规则需说明输入、输出、边界条件和验证方法。不得以实现方便为由改变核心体验。

### 客户端 Agent

负责首屏、附近厕所、评分、异常上报、榜单和导航入口。遵守 `spec.md` 的状态模型与可用性要求；不得在客户端自行复制一套评分或权限规则。

### 服务端/数据 Agent

负责厕所目录、评价写入、聚合、实时状态、反滥用、数据保留、审核和接口契约。必须保证重复提交、乱序到达、过期数据和并发更新下的确定性。

### 地图/定位 Agent

负责位置授权、距离计算、厕所坐标、开放状态和外部导航衔接。要明确定位失败、权限拒绝、室内误差和第三方服务不可用时的降级行为。

### 质量/安全 Agent

负责测试策略、异常流程、可访问性、隐私、安全、性能和上线验收。优先测试会误导用户、影响安全或导致数据失真的路径。

### 集成/发布 Agent

负责环境、构建、部署、监控、版本说明和回滚。不得把本地可运行等同于线上可用，必须提供可复现的验证命令和环境前提。

一个 Agent 可以兼任多个角色，但每个任务必须声明当前主责边界。

## 任务开始协议

开始工作前写清楚：

- 目标：要改变的用户行为或系统结果。
- 范围：涉及哪些文件、模块、接口或文档。
- 非目标：明确本次不处理的内容。
- 前置事实：来自文件、测试或外部资料的可验证信息。
- 假设：尚未验证但必须暂时采用的判断。
- 验收条件：完成后如何证明有效。
- 风险：可能造成的错误、隐私影响、数据偏差或回滚成本。

如果发现任务会改变 `spec.md` 的已批准规则，先提出决策记录；若用户已授权自主推进，则采用最小变更并记录理由。

## Skills 调用规范

- 只在任务与 Skill 描述明确匹配时调用 Skill，不为形式完整而调用。
- 先读 Skill 的 `SKILL.md`，理解输入、输出、限制和验证要求，再执行。
- Skill 产物必须回写到项目目录，并在交付中提供绝对路径和验证结果。
- 多个 Skill 组合时，先确定数据流和依赖顺序；不要让多个 Skill 重复生成同一事实。
- 外部研究必须标记来源与访问时间；无法验证的内容只能作为待验证假设。
- 生成媒体、文档或外部发布物时，先确认它属于本项目，再放入项目目录，不散落在无关目录。

当前项目在现有 Skill 清单中没有强制专用 Skill。后续涉及 PDF、Word、PPT、表格、部署或日历等具体产物时，按对应 Skill 触发规则读取。

## Memories 调用规范

- Memory 只用于恢复跨会话的稳定用户偏好、长期背景或重要协作约定，不用于存放本项目的状态、技术决策、代码细节或临时待办。
- 项目事实只写入项目文档；这样任何 Agent 都能在仓库内复现上下文，不依赖个人记忆。
- 读取 Memory 后只引用与当前任务相关的内容，并区分长期偏好与当前项目假设。
- 不把敏感个人信息复制到日志、Issue、埋点或交接文档。
- 新的长期协作偏好应通过记忆系统记录；本项目的决策仍须同步到项目文档。

## 交接格式

每次交接在对应 PR、任务记录或项目文档中包含以下结构：

```text
任务：一句话说明目标
状态：未开始 / 进行中 / 已完成 / 阻塞
完成：列出已经完成的可验证结果
变更：列出文件、接口、规则或数据结构变化
验证：命令、测试、截图、样本或人工检查结果
未决：仍需决定的问题
假设：尚未验证且会影响后续的判断
风险：已知风险、影响和建议处理方式
下一步：下一个 Agent 可直接执行的动作
```

交接禁止使用“基本完成”“应该没问题”等不可验证描述。若阻塞，必须写出阻塞原因、已尝试内容、所需输入和不依赖该输入的可并行工作。

## 决策记录规范

会改变用户体验、数据含义、安全边界、接口契约、依赖或成本的决定，都要记录：

- 背景与要解决的问题。
- 约束和候选方案。
- 选择及理由。
- 代价与风险。
- 验证计划。
- 日期、负责人和影响的文档/模块。

临时实验可以放在任务记录中；一旦成为产品规则，必须同步到 `spec.md`。
- 技术栈已冻结（spec.md §7：Workers + Hono + D1 + SolidJS）。除非用户明确要求，不重开技术栈讨论；认为有更优方案时，先写决策记录再讨论，不得私自替换实现。

## 完成交付标准

交付前检查：

- 任务范围没有无关扩张。
- 新规则与 `spec.md` 一致，或已记录冲突。
- README 的进度和待办已更新。
- 关键路径有针对性验证，不只做静态检查。
- 交接信息足以让没有本轮上下文的 Agent 继续工作。
- 用户可见文案、错误状态和降级路径已覆盖。
